/**
 * Internal implementation helpers for {@link io.github.parseworks.taker.Taker}.
 * <p>
 * This package is intentionally not exported by the JPMS module. Types here may
 * be public so the core package can call them, but they are not part of the
 * supported user API.
 */
package io.github.parseworks.taker.internal;
